import { SpaceRemoveDirective } from './space-remove.directive';

describe('SpaceRemoveDirective', () => {
  it('should create an instance', () => {
    const directive = new SpaceRemoveDirective();
    expect(directive).toBeTruthy();
  });
});
