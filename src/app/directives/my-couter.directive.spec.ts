import { MyCouterDirective } from './my-couter.directive';

describe('MyCouterDirective', () => {
  it('should create an instance', () => {
    const directive = new MyCouterDirective();
    expect(directive).toBeTruthy();
  });
});
